# 2026-08-16

## Git 工作流初始化（lifeos-BuildingAI-master 纳入版本管理）
- 将原本非 git 的本地目录纳入 git 管理：
  - clone 用户 fork `piepiepie/BuildingAI` (26.1.2) 到临时目录 `BuildingAI-tmp`
  - 配置 `upstream = BidingCC/BuildingAI`（官方），主分支 `master`
  - 开 `feature/init-codebuddy` 分支
  - 用 rsync（排除 .git/node_modules/.output/storage/logs/.turbo/.pnpm-store/.playwright-cli/.codebuddy）迁移现有二次开发（469 处改动，含 ai-agent-skill 系列、skill-parser、client contexts、skills.tsx、docs/）
  - 追加 .gitignore 忽略散落截图/草稿与 `public/web/assets`、`public/web/extensions` 构建产物
  - 提交基线 `e62a4167` 并 push 到 origin
- 目录切换：`lifeos-BuildingAI-master` → `lifeos-BuildingAI-master-bak`（备份），`BuildingAI-tmp` → `lifeos-BuildingAI-master`（git 仓库上位）
- 用同盘 `mv` 恢复 `node_modules`/`storage`/`.pnpm-store`/`.turbo` 到新目录
- 停并重启 Docker（lifeos-nodejs/postgres/redis）：API 4091 返回 HTTP 200，buildingai 进程 online
- 分支规范确认：二次开发后缀 `-codebuddy`
- 待办（用户操作）：GitHub 上从 `feature/init-codebuddy` 向 `master` 提 PR 合并；日后可 `git fetch upstream && git merge upstream/master` 同步官方更新

## 关于 chat 配置页的系列问答（本次会话前半段）
- `/console/chat/config` 是全局聊天页的界面/行为配置（欢迎语、建议、附件限制、MCP 详情、辅助模型路由），**无 system prompt 字段**
- 真正的智能体提示词在 AI 智能体（Agent）模块的 `rolePrompt` 字段
- 内置工具来自 `packages/@buildingai/ai-toolkit/src/tools/`（如天气 `getWeather`），由 service 的 `buildTools` 无条件注入；MCP/技能/供应商专属工具为其他扩展层次

## 离线文档知识库 + 专属技能 buildingai-docs（本次会话后半段）
- 把 `https://doc.buildingai.cc/llms.txt` 全部 **319 篇 `*.md`** 镜像到本地：`.codebuddy/knowledge/buildingai-help/`（含 `_index.md` 层级索引、`_llms_index.txt` 原始副本），共 1.9M，0 失败 0 空文件。
- 创建用户级技能 `~/.codebuddy/skills/buildingai-docs/SKILL.md`：开发时优先 `search_content` 检索本地语料，联网仅作兜底。
- 已在 `MEMORY.md` 写明「自动使用约定」+ 把 `.codebuddy/knowledge/` 加入 `.gitignore`（git status 已确认被忽略）。
- 验证：本地文档为完整原文（联网 web_fetch 会被摘要层压缩，本地镜像才是全量）；语料可检索。
- 设计取舍：选「本地镜像 + Skill」而非 RAG（RAG 库无法用工具创建）或每次联网抓（慢/依赖网络/被截断）。双保险（MEMORY 自动注入 + 技能显式触发）保证自动用、离线用。

## 二次开发代码审查 + B1-B4 修复（本次会话末段）
- 审查范围：`git diff 53cdce73 e62a4167`（feature/init-codebuddy 上 codebuddy 引入的 70 文件改动）。
- 报告：`.codebuddy/code-review-2026-08-16.md`。结论：2 High（B1 git clone SSRF/选项注入、B2 skillIds 越权）、5 Med（B3 迁移重复建表、B4 删 skill 孤儿引用、B5 createBy 空值越权、B6 skill 注入无长度上限、F1 third-party-integration.tsx 526 行过度膨胀未用 rhf+zod）、4 Low。
- 用户指出审查时**未调用 buildingai-docs 技能**对照规范 → 事后用本地语料核实：B8(throw new Error 应为 HttpErrorFactory)、B2(@IsUUID)、F1(rhf+zod)、B3、B9(console.log 应为 this.logger) 全部成立（依据文档 docid 8554940m0/8590372m0/8590368m0/8555860m0 等）。
- **已修复 B1-B4（直接改在 feature/init-codebuddy，未提交/未推送；用户拒绝另开 fix 分支）**：
  - B1 ai-agent-skill.service.ts 新增 `validateGitUrl()`（new URL 解析/仅 http-https/禁凭据/私有网段黑名单）+ git clone 加 `--` 终止符
  - B2 update-agent.dto.ts skillIds 改 `@IsUUID("4",{each:true})`；agents.service.ts 新增 `requireOwnedSkills()` 校验 skillIds 均属本 agent
  - B3 从 1774943726484-26.0.0-upgrade.ts 移除 ai_agent_skill 建表+索引+skill_ids 列（仅保留在 1776300000000）
  - B4 ai-agent-skill.service.ts remove() 同步清理 agent.skillIds；agents.service.ts deleteAgent 先删关联 skill 行
  - 验证：3 TS 文件 lint 0 错误；tsc 仅环境性 `Cannot find module '@buildingai/*'`（内部包未 build，非本次引入）；db 迁移 tsc 通过。
- 未修（待办）：B5(createBy 空值越权需定"谁可管系统 agent")、B6(长度上限)、B7(zip-slip)、B8/B9(统一异常/日志)、F1-F4(前端重构)。

## 代码审查第二批修复 B5-B9 / F1-F4（同会话续）
- 全部修完（直接改 feature/init-codebuddy，未提交/未推送），5 个文件 lint 0 错误。
- B5：ai-agent-skill.service.ts `requireOwnedAgent` 改为接收 `UserPlayground`；`createBy` 为空（系统/模板 agent）**仅 `isRoot===1` 可管理**，普通用户 forbidden；`createBy` 非空须 `=== user.id`。`listByAgent`/`remove` 签名同步改。`isRoot` 判定为"谁可管系统 agent"的临时定义。
- B6：prompt-builder.ts 注入 skill 数量上限 10 + 单条指令长度上限 8000（超长截断），超限 Logger.warn。
- B7：addByUpload 解压后新增 `assertNoZipSlip(tmpDir)` 递归校验 `realpath` 落于临时目录内，越界即拒绝。
- B8：ai-agent-skill.controller.ts `throw new Error("缺少上传文件")` → `HttpErrorFactory.badRequest`（报告原误标为 extension.controller）。
- B9：**核实误报**——coze-chat.provider.ts / coze-api.service.ts / coze-agent-sync.service.ts 均用框架 `this.logger`，无 console.log/自定义异常，无需改。
- F1：新建 `coze-config-parser.ts` 抽出 `extractCurlMeta`/`smartParseJson` 纯函数；注意当前 `third-party-integration.tsx` 已被重构为 `@buildingai/ui` + `memo` 受控组件（非报告旧 526 行版本）。
- F2：抽出统一 `SYSTEM_MANAGED_KEYS` 常量（原三处内联 systemKeys 数组）。
- F3：切换 v1↔v2 不再清空共用凭证 `apiKey`；新增 `apiKeyDraft` 本地状态，密码框不回显已存明文（仅输入非空才覆盖 apiKey），修明文回填+误清空。
- F4：移除全部 `Record<string,any>` → `Record<string,unknown>`。
- 报告 `.codebuddy/code-review-2026-08-16.md` 已追加第八、九节；原"待办清单"全部收口。F1 全面切 rhf+zod、F2 schema 驱动抽象未做整体重写（风险高、当前受控组件合理），如需可单列任务。
